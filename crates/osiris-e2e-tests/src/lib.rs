//! Workspace integration tests spanning multiple crates (ARCHITECTURE.md
//! §24's `tests/` directory) — Phase 1's end-to-end exec-chain trace and
//! pipeline throughput smoke test. No public API; see the `tests/` files.
